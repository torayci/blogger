<%= form_for(@article) do |f| %>
	<ul>
	<% @article.errors.full_messages.each do |error| %>
		<li><%= error %></li>
	<%end%>
	</ul>
	<p>
	<%= f.label "Title" %>
	<%= f.text_field :title %>
	</p>
	<p>
	<%= f.label "Body" %>
	<%= f.text_area :body %>
	</p>
	<p>
	<%= f.submit %>
	</p>
	<%end%>